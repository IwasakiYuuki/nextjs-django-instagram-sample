module.exports = {
  jwt: {
    // とりあえず開発用で適当な値をおいておく
    access_secret: 'access_secret_key',
    access_secret_expire: '1h',
    refresh_secret: 'refresh_secret_key',
    refresh_secret_expire: '7d'
  }
}
